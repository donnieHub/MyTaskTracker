server:
  port: 8081
  error:
    include-message: always

spring:
  application:
    name: user-service

  datasource:
    url: jdbc:postgresql://localhost:54325/userdb
    username: user
    password: user

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update